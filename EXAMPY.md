# exampy

## Installation / Setup

- git clone this repository as `root` into `/root` (or just extract the zipfile/tarball)
- `.profile`: export PATH=~/unbound-onlywhite/bin:$PATH
- symlink the an etc dir: `[root@spgplf ~/unbound-onlywhite]# ln -s etc-georg etc`
